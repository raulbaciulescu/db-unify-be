spring:
  application:
    name: db-unify-be
  datasource:
    url: jdbc:postgresql://localhost:5432/bootstrapdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update