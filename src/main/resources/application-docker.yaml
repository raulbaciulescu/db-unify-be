spring:
  application:
    name: db-unify-be
  datasource:
    url: jdbc:postgresql://pg-flexible-server-db-unify.postgres.database.azure.com:5432/boostrapclouddb
    username: pgadmin
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update